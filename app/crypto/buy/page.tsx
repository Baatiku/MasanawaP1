import Link from "next/link";
import { ArrowLeft, Bitcoin, ShieldCheck } from "lucide-react";
import { createCryptoBuyOrder } from "../actions";

export default async function BuyCryptoPage({ searchParams }: { searchParams?: Promise<{ error?: string }> }) {
  const params = (await searchParams) ?? {};
  return <main className="min-h-screen px-5 py-6 md:px-8 lg:py-10"><div className="mx-auto max-w-2xl"><Link href="/crypto" className="muted inline-flex items-center gap-2 text-sm hover:text-white"><ArrowLeft size={17}/>Back to crypto</Link><div className="mt-7"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-300"><Bitcoin size={22}/></div><h1 className="mt-5 text-2xl font-bold md:text-3xl">Buy crypto</h1><p className="muted mt-2 text-sm">Create a secure pending order. Provider pricing is applied only on the backend.</p></div><section className="panel mt-7 rounded-[30px] p-5 md:p-7">{params.error && <div className="mb-5 rounded-2xl border border-rose-400/20 bg-rose-400/8 px-4 py-3 text-sm text-rose-200">{params.error}</div>}<form action={createCryptoBuyOrder} className="space-y-5"><div><label htmlFor="asset" className="text-xs font-semibold">Asset</label><select id="asset" name="asset" required defaultValue="USDT" className="mt-3 w-full rounded-2xl border border-white/8 bg-[#101a2b] px-4 py-3.5 text-sm outline-none"><option value="USDT">USDT · Tether</option><option value="BTC">BTC · Bitcoin</option><option value="ETH">ETH · Ethereum</option></select></div><div><label htmlFor="amount" className="text-xs font-semibold">You pay (NGN)</label><div className="mt-3 flex rounded-2xl border border-white/8 bg-white/[.035]"><span className="px-4 py-3.5 text-sm font-semibold text-slate-400">₦</span><input id="amount" name="amount" required type="number" min="100" step="100" defaultValue="50000" className="min-w-0 flex-1 bg-transparent px-1 py-3.5 text-sm outline-none"/></div></div><div className="soft-panel rounded-2xl p-4"><div className="flex justify-between text-xs"><span className="muted">Order state</span><span>Pending provider quote</span></div><div className="mt-3 flex justify-between text-xs"><span className="muted">Final rate & fee</span><span>Server controlled</span></div></div><div className="flex items-start gap-3 rounded-2xl bg-cyan-300/[.045] p-4"><ShieldCheck className="mt-0.5 shrink-0 text-cyan-300" size={17}/><p className="muted text-xs leading-5">Masanawa validates your authenticated NGN wallet balance before creating the order. This step does not settle or debit the wallet; provider-confirmed settlement remains server-only.</p></div><button type="submit" className="flex w-full items-center justify-center rounded-2xl bg-cyan-300 py-3.5 text-sm font-bold text-slate-950">Create crypto buy order</button></form></section></div></main>;
}
